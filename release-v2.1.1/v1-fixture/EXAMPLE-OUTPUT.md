# Example verification output

The following is a compact successful run on the v1 fixture corpus:

```text
standalone_releases: 8
author_endorsements: 18
series_signed_objects: 7
scenarios: 13
profile checks: 30 passed, 0 failed
public artifact manifest: 111 passed, 0 failed
```

The exact JSON reports are regenerated under `artifact/artifacts/` and
`artifact/evidence/` by `run.ps1` or `run.sh`.
